services:
  mongodb:
    image: mongo:7
    container_name: aad-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: adventurers-action-deck

volumes:
  mongo-data:
